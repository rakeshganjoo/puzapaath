# shared-components

Reusable UI primitives shared by 2+ MFEs.

## Rules
- UI-only components here.
- No business logic, no storage access.
- Shared logic belongs in `src/services` and `src/contexts`.
