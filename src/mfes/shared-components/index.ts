/**
 * shared-components — Cross-MFE UI primitives and atoms.
 *
 * Only put things here if they are:
 *   1. Used by 2+ MFEs, AND
 *   2. Stateless (or manage only UI state — no business logic)
 *
 * Shared contexts (not UI components) live in src/contexts/.
 * Shared services live in src/services/.
 * Shared types live in src/types/.
 * Shared data live in src/data/.
 *
 * TODO Phase 5: extract common header, button, card, modal components here
 * as they're pulled out of individual screens during the cleanup pass.
 */

// Add exports here as shared components are extracted.
export {};
